const APP_NAME = 'app-v1.0.7';
// IMPORTANT: Everytime when you upgrade muso search version,
// don't forget to notify Kyran about it. Otherwise, search will break on landing page,
// since we store on our server only the lastest version of our search component.
// It means that previous version gets removed.
const SEARCH_APP_NAME = 'search-v0.0.5';

module.exports = {
  APP_NAME,
  SEARCH_APP_NAME
};
